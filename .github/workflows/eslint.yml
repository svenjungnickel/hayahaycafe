name: Run ESLint
on:
  pull_request:
    branches:
      - master
jobs:
  eslint:
    name: Linting
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: Use Node.js 10.x
        uses: actions/setup-node@v1
        with:
          node-version: 10.x
      - name: Run ESLint
        run: |
            npm install -D
            npm run lint